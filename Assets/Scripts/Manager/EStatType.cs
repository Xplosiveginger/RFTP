using UnityEngine;

/// <summary>
/// Enum defining all available player stats
/// </summary>
public enum EStatType
{
    Health,
    MoveSpeed,
    Damage,
    Defense,
    JumpHeight,
    MaxHealth,
    HealthRegen
}
